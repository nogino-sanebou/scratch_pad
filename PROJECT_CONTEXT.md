# scratch pad 仕様書

## アプリ概要
このアプリは保存不要のメモ帳です。

名称は「scratch pad」です。

成果物はWindowsストアに公開します。

以下の機能を保有しています。

上部:
- タブバー
- 右端に + ボタン

ツールバー:
- タブ一覧
- 保存
- タブ名変更
- フォントサイズ
- 背景変更

タブ一覧モーダル:
- タイトル
- 表示状態
- 表示/非表示
- 名前変更
- 削除

保存:
- Ctrl+S
- アプリ終了時
- 1分ごと

設定:
- フォントサイズ
- 背景色
- 背景画像パス
- 背景画像不透明度

あまりリッチな機能は付けず、気軽に使えるメモ帳を想定しています。

## プロジェクト構成
プロジェクト共有のSTRUCTURE.mdを参照して下さい。

ただし、rsファイルなど必要であれば委員長の方で追加して構いません。

## 実装方針
基本的にRustで済ませたいのでJavaScriptの実装は最小限に済ませたいです。

巨大なコンポーネントやネストにならないようにしたいと思います。

具体的には
- コンポーネントを細かく分かる
- onイベントはできるだけコンポーネントの外に置く

その他このファイルに載っていなくて必要な事があれば
都度教えて下さい。PROJECT_CONTEXT.mdに方針を追記します。

## 実装スケジュール

### フェーズ１
- テキスト入力エリア
- ツールバー(タブ一覧、設定)
- 多言語対応(準備)

ツールバーのボタンはスケルトンだけ作成し、
実際のボタン押下時の動作は各フェーズで実装します。

多言語対応は大枠をここで作り、
次のフェーズ以降個別に対応します。
対応する言語は日本語と英語です。

### フェーズ２
タブバー
- 右端に+ボタンでタブを追加、タブ一覧に追加
- タブタイトル右の×ボタンでタブ削除(タブ一覧からは削除しない)

### フェーズ３
タブ一覧
- タブの名称で一覧を表示する
- 名称の右に各機能のボタンを表示する
- タブの表示/非表示切り替え
- タブの名前を変更
- タブの完全削除
- アプリ終了時に設定を保存
- アプリ起動時に設定を復元

### フェーズ４
保存
- Ctrl + Sで保存
- アプリ終了時に保存
- アプリ起動時に復元
- 1分毎に自動保存

## フェーズ５
設定画面

以下の設定をできるようにモーダル画面のスケルトンを作成
- 言語設定
- フォントサイズ
- 背景色
- 背景画像

## フェーズ６
言語設定

- 英語と日本語を切り替える
- 初回起動時はOSの言語が日本語の場合は日本語に、それ以外は英語にする
- アプリ終了時に設定を保存
- アプリ起動時に設定を復元

## フェーズ５
フォントサイズ
- ツールバーにコンボボックスを実装し、50%～200%で変化する
- Ctrl + マウスホイールでもサイズが変更できる
- アプリ終了時に設定を保存
- アプリ起動時に設定を復元

## フェーズ６
背景色
- カラーピッカーで色を設定する(今回は文字色までは考慮しない)
- アプリ終了時に設定を保存
- アプリ起動時に設定を復元

## フェーズ７
背景画像
- 指定のパスの画像を背景に表示する(全タブ共通)
- 不透明度を設定できる(デフォルトで15%)
- アプリ終了時に設定を保存
- アプリ起動時に設定を復元